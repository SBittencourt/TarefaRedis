<h1> Como rodar o programa: </h1>

Abra o terminal na pasta em que o repositório foi clonado e digite:

`pip install pymongo`

`pip install redis`

`pip install bson`

`cd sistema`

`python .\menu.py`
